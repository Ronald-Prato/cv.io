### cv.io | Project Setup

First screen should be the home route, which is the first place where users can interact with the UI and the agent, is divided in 2 main areas: 

*1. Left Side Bar*
It has user's info (name and email) on top. Then a divider then a "My CVs" section underneath with my created CVs.

*2. Right Side Interactive Chat*
Very ChatGPT styled chat, which can work as a drop zone as well. It allows PDFs upload and a simple chat textarea 
